# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
.vscode/

# macOS
.DS_Store
Screenshot*.png

# Wrangler local state (wrangler.jsonc itself IS committed)
.wrangler/
node_modules/

# Platform build artefacts
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
macos/Pods/
android/.gradle/
android/local.properties

# NOTE: public/ is deliberately NOT ignored. It holds the built site that
# Cloudflare Pages serves directly, so it has to be committed.
